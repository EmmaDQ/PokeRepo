@page
@using PokeRepo.Models
@model PokeRepo.Pages.Gen1Model
@{
}


<div class=" text-center">
    <form method="post">
        @foreach (var name in Enum.GetNames(typeof(Models.Gen1)))
        {
            <div>
                <a asp-page="/Details" asp-route-name="@name"><h1>@name</h1></a>
                <hr />
                <br />
            </div>
            
        }
        
    </form>
    
    
</div>
